name: Broken CI Pipeline

on:
  push:
  branches: main   # ❌ wrong indentation

jobs:
  test:
    runs-on: ubuntu   # ❌ wrong runner
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
      uses: actions/setup-python@v5   # ❌ indentation error
        with:
          python-version: 3.9

      - name: Install deps
        run: |
          pip install -r requirements.txt
          pip install pytest

      - name: Run Tests
        run: pytest tests/ -v

  build:
  runs-on: ubuntu-latest   # ❌ wrong indentation

    steps:
      - uses: actions/checkout@v4

      - name: Build Image
        run: docker build -t myapp .

deploy:
runs-on: ubuntu-latest   # ❌ missing indentation
needs: test
steps:
  - name: Deploy
    run: echo Deploying...
